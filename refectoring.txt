 
private boolean isSumInBounds(int minValue, int maxValue, int first, int second) {
    if (minValue > 0 && maxValue < 100) {
        int sum = first + second;        
        if (minValue < sum && sum < maxValue) {
            return true;        
        }
    }
    return false;
}
